import { StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  homeIcon: {
    backgroundColor: '#ffffff',
    position: 'absolute',
    top: '5%',
    left: '5%',
    zIndex: 50,
    borderRadius: 50,
    padding: 10,
  },
  boxMap: {
    height: '50%',
  },
  boxMapNavigator: {
    height: '50%',
  }
})
